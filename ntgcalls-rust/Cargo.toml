[package]
name = "ntgcalls"
version = "2.0.0"
edition = "2021"

[lib]
crate-type = ["cdylib"]

[dependencies]
napi = { version = "2.16", features = ["async", "tokio_rt"] }
napi-derive = "2.16"
tokio = { version = "1", features = ["sync"] }

[build-dependencies]
napi-build = "2.1"
